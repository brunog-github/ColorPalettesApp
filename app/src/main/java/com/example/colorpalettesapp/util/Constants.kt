package com.example.colorpalettesapp.util

import com.example.colorpalettesapp.BuildConfig

object Constants {

    const val APP_ID = BuildConfig.APP_ID
    const val API_KEY = BuildConfig.API_KEY

    const val CLIENT_ID = BuildConfig.CLIENT_ID
    const val CLIENT_SECRET = BuildConfig.CLIENT_SECRET

    const val REDIRECT_URI = BuildConfig.REDIRECT_URI

    const val COLOR_PALETTE_KEY = "colorPalette"
}